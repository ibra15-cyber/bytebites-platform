server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ibra15-cyber/bytebites-config.git
          default-label: master
          username: ${GIT_USERNAME}
          password: ${GIT_PAT}